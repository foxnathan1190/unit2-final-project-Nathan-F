export default class Brewery {
    constructor (id, name, city, state, websiteUrl) {
        this.id = id;
        this.name = name;
        this.city = city;
        this.state = state;
        this.website_url = websiteUrl;
    }
}